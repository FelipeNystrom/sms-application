{
  "name": "send-sms-service",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon -r dotenv/config server.js",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "client-dev-start": "cd client && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "nexmo": "^2.4.0"
  }
}
